<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ModernFoodRecipesApp" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomChipStyle" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipBackgroundColor">@color/chipBackgroundColor</item>
        <item name="android:textColor">@color/chipTextColor</item>
        <item name="checkedIconTint">@color/chipTextColor</item>
    </style>

    <style name="Theme.ModernFoodRecipesApp" parent="Base.Theme.ModernFoodRecipesApp" />
</resources>